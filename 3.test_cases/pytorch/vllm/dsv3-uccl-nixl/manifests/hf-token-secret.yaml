# Apply with: envsubst < manifests/hf-token-secret.yaml | kubectl apply -f -
apiVersion: v1
kind: Secret
metadata:
  name: hf-token
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  HF_TOKEN: "${HF_TOKEN}"
